# Database Configuration (use 'localhost' for local dev, 'mysql' for Docker)
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=indicadores123
DB_DATABASE=indicadores_chile
DB_SYNC=false
DB_LOGGING=false

# Redis Configuration (use 'localhost' for local, 'redis' for Docker)
REDIS_URL=redis://localhost:6379

# Cache
CACHE_TTL=3600

# Application
NODE_ENV=development
PORT=3000
